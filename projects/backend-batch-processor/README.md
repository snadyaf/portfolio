# Processador de dados

Projeto backend em JavaScript que analisa registros e gera métricas de uso.

## Como rodar

```bash
cd projects/backend-batch-processor
npm install
node processor.js
```

O script lê dados em `sample-data.json` e escreve um relatório em `output-report.json`.
